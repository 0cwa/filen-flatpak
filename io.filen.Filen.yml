app-id: io.filen.Filen

runtime: org.freedesktop.Platform
runtime-version: &runtime-version '24.08'
sdk: org.freedesktop.Sdk

base: org.electronjs.Electron2.BaseApp
base-version: *runtime-version

command: startfilen
separate-locales: false

finish-args:
  - --socket=pulseaudio
  - --socket=wayland
  - --socket=fallback-x11
  - --share=ipc
  - --share=network
  - --talk-name=org.kde.StatusNotifierWatcher # Tray functionalities on KDE
  - --talk-name=com.canonical.AppMenu.Registrar
  - --filesystem=home
  - --env=XCURSOR_PATH=/run/host/user-share/icons:/run/host/share/icons # This is used to show the correct cursor on Wayland

modules:
  - name: Filen
    buildsystem: simple
    build-commands:
      - chmod +x Filen.AppImage
      - ./Filen.AppImage --appimage-extract
      - desktop-file-edit --set-key="Exec" --set-value="startfilen %U" --set-icon=$FLATPAK_ID
        squashfs-root/@filendesktop.desktop
      - |
        for icon in 16 24 32 48 64 128 256 512; do
          install -D squashfs-root/usr/share/icons/hicolor/${icon}x${icon}/apps/@filendesktop.png \
              /app/share/icons/hicolor/${icon}x${icon}/apps/io.filen.Filen.png
        done
      - install -D squashfs-root/@filendesktop.desktop /app/share/applications/io.filen.Filen.desktop
      - install -Dm755 startfilen /app/bin/startfilen
      - install -D io.filen.Filen.metainfo.xml -t /app/share/metainfo/
      - mv squashfs-root /app/bin/filen

    sources:
      - type: file
        path: startfilen

      - type: file
        path: io.filen.Filen.metainfo.xml

      - type: file
        url: https://github.com/FilenCloudDienste/filen-desktop/releases/download/v3.0.47/Filen_linux_x86_64.AppImage
        dest-filename: Filen.AppImage
        sha256: 91e683e4f52392816b1530ae6a9e03be662ae57dd38e7abe9e3b622e0019f56f
        only-arches: [x86_64]
        x-checker-data:
          type: electron-updater
          url: https://github.com/FilenCloudDienste/filen-desktop/releases/latest/download/latest-linux.yml

      - type: file
        url: https://github.com/FilenCloudDienste/filen-desktop/releases/download/v3.0.47/Filen_linux_arm64.AppImage
        dest-filename: Filen.AppImage
        sha256: e28d9fbe1458f7050647bf6117ac8182d9bac851239e5f49fb241d74597e6ac7
        only-arches: [aarch64]
        x-checker-data:
          type: electron-updater
          url: https://github.com/FilenCloudDienste/filen-desktop/releases/latest/download/latest-linux-arm64.yml
